{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:product-template-converter:release-matrix-run-spec:v3",
  "title": "ReleaseMatrixRunSpecV3",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "manifest", "cases"],
  "properties": {
    "schema_version": {"const": "release-matrix-run-spec-v3"},
    "manifest": {"$ref": "#/$defs/artifactRef"},
    "cases": {
      "type": "array",
      "minItems": 16,
      "maxItems": 16,
      "items": {"$ref": "#/$defs/case"}
    }
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[A-F0-9]{64}$"},
    "artifactRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "sha256"],
      "properties": {
        "path": {"type": "string", "minLength": 1},
        "sha256": {"$ref": "#/$defs/sha256"}
      }
    },
    "sourceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "sha256", "format"],
      "properties": {
        "path": {"type": "string", "minLength": 1},
        "sha256": {"$ref": "#/$defs/sha256"},
        "format": {"enum": ["DOC", "DOCX", "PPT", "PPTX", "PDF"]}
      }
    },
    "templatePack": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "version", "template_sha256"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "version": {"type": "string", "minLength": 1},
        "template_sha256": {"$ref": "#/$defs/sha256"}
      }
    },
    "case": {
      "type": "object",
      "additionalProperties": false,
      "required": ["case_id", "source", "template_pack", "content_decision", "review_receipt", "delivery_target"],
      "properties": {
        "case_id": {"type": "string", "minLength": 1},
        "source": {"$ref": "#/$defs/sourceRef"},
        "template_pack": {"$ref": "#/$defs/templatePack"},
        "content_decision": {"$ref": "#/$defs/artifactRef"},
        "review_receipt": {"$ref": "#/$defs/artifactRef"},
        "delivery_target": {"enum": ["desktop", "mobile"]}
      }
    }
  }
}
